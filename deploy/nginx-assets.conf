# Add this inside your existing server {} block
# Serves uploaded files as public static assets
location /assets/ {
    alias /var/www/assets/;
    add_header Cache-Control "public, max-age=31536000, immutable";
    try_files $uri =404;
}
